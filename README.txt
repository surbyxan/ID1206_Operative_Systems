To compile the sourcecode:
gcc task1.c -o task1

To run the code from the makefile:
make run // runs the file, N = 100
make run N = k // runs the file and sets N to k, where k is the desired value of N
make clean // cleans up the compiled file
